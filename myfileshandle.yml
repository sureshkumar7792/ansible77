---
 - hosts: webservers
   become: yes
   tasks: 
   - include_tasks: myfile.yml
   - name: install apache2 on ubuntu
     apt:
      name: "{{iub}}"
      state: "{{pr}}"
     when: a_os_family =="Debian"
   - name: start apache2 server
     servie: 
      name: "{{iub}}"
      state: started
     when: a_os_family =="Debian"   
   - name: copy index.j2 file into anisble node side 
     template:
      src: index.j2
      dest: /var/www/html/index.html


  
